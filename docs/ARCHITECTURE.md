# Architecture

This document will describe the full pipeline (ingestion → embedding → entity graph → hybrid
search → graph expansion → rerank → explain → evaluate) as each piece is built. Filled in
incrementally through the build phases; finalized in Phase 8.
